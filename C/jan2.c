#include<stdio.h>
/*
1. Take marks of 3 subjects (sub 0, sub 1 & sub 2) for 5 students from the user (all integers) and then ask user that marks of which subject and for which student (stu 0,1,2,3 or 4) do they want to see and print only marks of that subject scored by that student.
*/
/*
int main()
{
    int r[5][3];
    int i, j, stu, sub;
    for(i=0; i<5; i++){
        printf("Enter marks of student-%d:\n", i);
        for(j=0; j<3; j++){
            printf("Sub-%d: ", j);
            scanf("%d", &r[i][j]);
        }
    }
    
    for(i=0; i<5; i++){
        printf("\nMarks of student-%d:\n", i);
        for(j=0; j<3; j++){
            printf("Sub-%d: %d\n", j, r[i][j]);
        }
    }
    
    printf("Student no: ");
    scanf("%d", &stu);
    printf("Subject no: ");
    scanf("%d", &sub);
    printf("Marks: %d", r[stu][sub]);
    return 0;
}
*/

/*
Create two 3x4 arrays. Take the inputs for both the arrays from user (all integers). Store them in those arrays appropriately then, add corresponding elements of both the arrays and store the result in a third array. print all the arrays at the end.
*/

/*
Create a 4x3 array and store the 12 numbers (float) given by user in it. Now print the sum of all the elements of this array.
*/